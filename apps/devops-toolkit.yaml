---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: devops-toolkit-staging
  namespace: flux-system
spec:
  chart:
    spec:
      chart: helm
      sourceRef:
        kind: GitRepository
        name: devops-toolkit
  interval: 31s
  targetNamespace: staging
  values:
    image:
      tag: 0.1.2
    ingress:
      host: staging.devops-toolkit.10.0.0.71.nip.io

